# Qwen Council — Multi-Agent Code Review System

**Track 3: Agent Society** — Global AI Hackathon Series with Qwen Cloud

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

---

## What is Qwen Council?

Qwen Council is a **multi-agent system** where 6 specialised AI agents debate collaboratively to perform code review. Each agent has a unique expertise (Security, Architecture, Quality, Performance, UX, Vision) and follows a structured **Inverted Pyramid communication protocol** inspired by cognitive linguistics — making debates efficient, explicit, and self-sufficient. After 3 rounds of debate, a final **LLM-powered report** is generated with executive summary, risk overview, remediation roadmap, and agent metrics.

### Key Innovations

| Innovation | Description |
|:-----------|:------------|
| Linguistic Protocol | Inter-agent messages follow Inverted Pyramid (finding, detail, impact, proposal) with Given-New referencing for high cohesion |
| 3-Level Memory | Working (volatile) -> Episodic (PostgreSQL with forgetting curve) -> Semantic (pgvector embeddings) |
| Consensus Engine | Cross-agent voting with weighted consensus scoring |
| Debate Cycle | 3 rounds: Individual Analysis, Cross-Debate, Final Refinement |
| LLM-Powered Report | Executive summary, risk heatmap, remediation roadmap generated by qwen-coder-plus |

### Agents

| Agent | Expertise | Focus Area |
|:------|:----------|:-----------|
| Security | OWASP Top 10, SQL injection, XSS, hardcoded secrets, auth flaws | Vulnerability detection |
| Architecture | SOLID, coupling, scalability, design patterns | System design quality |
| Quality | Code style, dead code, complexity, tests, best practices | Code maintainability |
| Performance | N+1 queries, caching, inefficient loops, memory usage | Runtime efficiency |
| UX | Accessibility, i18n, error messages, contrast, usability | User experience |
| Vision | Screenshot/diagram analysis (qwen-vl-plus) | Visual inspection |

---

## Architecture

```
                    REACT FRONTEND (ChatGPT-style UI)
  Sidebar (sessions) | Chat messages | File upload | Follow-up Q&A
                            |
                     HTTP (REST API)
                            |
                    FASTAPI BACKEND
  POST /api/review  |  POST /api/chat  |  GET /api/sessions
                            |
          ------------------+------------------
          |                                  |
     COUNCIL ORCHESTRATOR             MEMORY SYSTEM
     Round 1: Individual Analysis    Working (in-memory)
     Round 2: Cross-Debate           Episodic (PostgreSQL)
     Round 3: Final Refinement       Semantic (pgvector)
          |
     LLM SYNTHESIZER (qwen-coder-plus)
     Executive Summary + Risk Overview +
     Detailed Review + Remediation Roadmap
          |
                    QWEN CLOUD API
  https://dashscope-intl.aliyuncs.com/compatible-mode/v1
  Models: qwen-coder-plus, qwen-vl-plus, text-embedding-v3
```

---

## Quick Start

### Prerequisites

- Python 3.11+
- Node.js 18+
- Docker + Docker Compose (optional, for production)
- Qwen Cloud API key

### 1. Clone & Setup

```bash
git clone https://github.com/02NIN20/qwen-council.git
cd qwen-council

# Backend
pip install -r backend/requirements.txt
cp .env.example .env
# Edit .env and set your qwen_api_key

# Frontend
cd frontend
npm install
```

### 2. Run Locally (Development)

```bash
# Terminal 1: Backend (from project root)
PYTHONPATH=. uvicorn backend.main:app --reload --port 8000

# Terminal 2: Frontend
cd frontend
npm run dev
```

Open http://localhost:5173

### 3. Run with Docker (Production)

```bash
# Copy and configure environment
cp .env.example .env
nano .env   # Set your qwen_api_key

# Build and start all services
docker compose up --build -d
```

The frontend will be available on `http://localhost:5173` and the API on `http://localhost:8000`.

---

## API Endpoints

| Method | Endpoint | Description |
|:-------|:---------|:------------|
| `POST` | `/api/review` | Submit code for council review (supports multiple files + instructions) |
| `POST` | `/api/chat` | Follow-up questions about a completed review |
| `GET` | `/api/sessions` | List past review sessions |
| `GET` | `/api/sessions/{id}` | Get session details and findings |
| `GET` | `/api/memory/patterns` | Get consolidated semantic memory patterns |
| `GET` | `/api/health` | Health check |

### POST /api/review

```json
{
  "code": "def hello():\n    return 1",
  "files": [
    {
      "filename": "main.py",
      "content": "def hello():\n    return 1",
      "language": "python"
    }
  ],
  "instruction": "Focus on security vulnerabilities",
  "image_url": "data:image/png;base64,..."
}
```

### Response

```json
{
  "session_id": "ses-abc123def456",
  "report": {
    "summary": "The code review identified critical security issues...",
    "risk_overview": "Critical risks include SQL injection...",
    "detailed_review": "1. [Critical] SQL Injection...",
    "remediation_roadmap": "1. Use parameterized queries...",
    "agent_metrics": {
      "per_agent": {
        "security": { "findings_count": 9, "top_severity": "Critical" }
      }
    },
    "findings": [
      {
        "title": "SQL injection vulnerability",
        "impact": "Critical",
        "votes": { "security": "Critical", "architecture": "High" },
        "consensus_level": "High",
        "consensus_score": 1.0
      }
    ]
  }
}
```

---

## Communication Protocol

Each agent message follows the **Inverted Pyramid** format:

```
FINDING: SQL injection vulnerability at user input handling
... Detail: src/app.py line 45: cursor.execute(f"SELECT * FROM users WHERE id = {user_input}") (CWE-89)
... Impact: Critical
... Proposal: Use parameterised queries. BEFORE: cursor.execute(f"SELECT...{user_input}") AFTER: cursor.execute("SELECT...WHERE id = ?", (user_input,))
```

In rounds 2+, agents apply **Given-New** cross-referencing:

```
FINDING: Agreeing with Security on SQL injection at line 45, I found the same pattern at line 78
... Detail: src/app.py line 78: same f-string pattern in delete_user() (CWE-89)
... Impact: Critical
... Proposal: Create a safe_query() helper that always uses parameterisation
```

---

## Deployment on Alibaba Cloud ECS

### Prerequisites

- An Alibaba Cloud ECS instance (Ubuntu 22.04+, 2GB RAM minimum, 4GB recommended)
- SSH access to the instance
- A Qwen Cloud API key

### Deploy

```bash
# SSH into your ECS instance
ssh root@your-ecs-ip

# Install git if not present
apt-get update && apt-get install -y git

# Clone the repository
git clone https://github.com/02NIN20/qwen-council.git
cd qwen-council

# Configure environment
cp .env.example .env
nano .env   # Set qwen_api_key

# Run the deployment script
sudo bash deploy.sh
```

The deploy script will automatically:
1. Install Docker and Docker Compose if missing
2. Build all Docker images
3. Start PostgreSQL, Backend API, and Frontend with Nginx
4. Run health checks
5. Execute a smoke test to verify the council works

The application will remain running with `restart: unless-stopped` policy.

### Architecture on ECS

```
                         Alibaba Cloud ECS
  ====================================================
  |  Docker Compose                                  |
  |  -------------------------------------------------|
  |  | Nginx (port 80)  |                             |
  |  |  -> /api/* -> backend:8000                     |
  |  |  -> /*    -> /usr/share/nginx/html             |
  |  |_______________________________________________|
  |  | FastAPI (port 8000) |                          |
  |  |  - CouncilOrchestrator                         |
  |  |  - LLM Synthesis                               |
  |  |  - Memory Managers                             |
  |  |_______________________________________________|
  |  | PostgreSQL (port 5432) |                       |
  |  |  - Episodic Memory                             |
  |  |  - Semantic Memory (pgvector)                  |
  |  |_______________________________________________|
  ====================================================
```

---

## Report Structure

The final report contains 5 sections:

1. **Executive Summary** — 2-3 paragraph overview for engineering leadership
2. **Risk Overview** — Risk heatmap of critical and high-severity issues with business impact
3. **Detailed Review** — Per-finding analysis with line numbers, CWE references, and code evidence
4. **Remediation Roadmap** — Prioritized fix plan with effort estimates
5. **Agent Metrics** — Per-agent statistics (findings found, severity distribution)

---

## Built for

[Global AI Hackathon Series with Qwen Cloud](https://qwencloud-hackathon.devpost.com/) — Track 3: Agent Society

---

## License

MIT
